
name: Build and test

on:
  pull_request:
    branches: [ master, jei-searchtree ]
  push:
    branches: [ master, jei-searchtree ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
